# 文本层质检 & OCR 提速：实测结论

> 2026-09-23 · 全部数据来自本机实测，脚本在 `work/` 下可复跑。
> 结论先行，证据在后。**含两处对我自己先前判据的推翻，已标注。**

---

## 一、两个问题的直接答案

**Q1：PaddleOCR-VL-1.5 能不能用来并行提速？**
**不能。** 它比 DeepSeek-OCR **慢 6 倍**（串行 p50 **70.3 s/页** vs **11.75 s/页**），
而且这个慢**与图片大小无关**（96/150/300 DPI 都是 85~95 s，见下表）。
它也不能顶替 DeepSeek-OCR —— 它的输出**没有 `[[x1,y1,x2,y2]]` 版面坐标**，
而 pdf-craft 正是靠 grounding 坐标切图还原版面的，换它**插图会全丢**。

但它有一个**真价值**，只是不在"提速"上（见第三节）。

**Q2：用文字模型从上下文检查多字/少字/错字，行不行？**
**行，而且是目前最有效的一层** —— 但要**规则先行、模型复核**，不能单用。
实测 4 个不同家族的文字模型，对少字/多字/错字/多余符号的**命中率 83%~90%**。

---

## 二、提速实测（`work/_ocr_speed_ab.py` / `_paddle_dpi.py`）

### 2.1 单页串行延迟（吞吐的分母）

| 引擎 | p50 | min | max | 样本 |
|---|---|---|---|---|
| **DeepSeek-OCR** | **11.75 s** | 8.91 s | 18.35 s | 6/6 |
| **PaddleOCR-VL-1.5** | **70.29 s** | 46.94 s | 98.50 s | 6/6 |

### 2.2 慢是因为图大吗？不是

同一页、同一提示，只改渲染分辨率：

| DPI | PNG 大小 | base64 | 耗时 |
|---|---|---|---|
| 300 | 160 KB | 214 KB | 85.5 s |
| 150 | 269 KB | 359 KB | 90.8 s |
| 96 | 131 KB | 175 KB | 95.0 s |

**降 DPI 完全不救** ⇒ 瓶颈是**模型自身推理/排队**，不是传输或预处理。
（对照：DeepSeek-OCR 在同一张 300 DPI 图上 8.9~18.4 s。）

### 2.3 24 路并发吞吐与"叠加"

| 阶段 | 配置 | 吞吐 |
|---|---|---|
| 2 | DeepSeek-OCR 24 路（key1） | 17.1 页/分 |
| 3 | PaddleOCR-VL 24 路（key2） | 13.2 页/分 |
| 4 | **两条同时开** | DeepSeek 臂 **67.7** + Paddle 臂 13.2 → 合计 **26.4 页/分** |

⚠️ **这张表里的 17.1 与 67.7 是同一配置的两次测量，差 4 倍** —— 说明
SF 免费层的服务端排队**波动极大**，且测量期间生产的 24 个 pdf-craft worker
一直在抢 key1。**单次吞吐测量不可信，结论只能取"量级与相对关系"**：

* Paddle 臂始终 ≈ 13 页/分（受它 70 s 延迟锁死），**加它 = +13 页/分**；
* 同期只压 DeepSeek 的臂能跑到 68 页/分。

⇒ **把并发全部压在 DeepSeek-OCR 上，收益远大于开第二条慢通道。**
（下一步该测的是 DeepSeek-OCR 在 32/48/64 路下的扩展性，而不是再找引擎。）

---

## 三、PaddleOCR-VL 的正确位置：**精修引擎**，不是提速引擎

实测它把公式还原成 LaTeX 的质量**很好**（这页正是 A 类判为 `math_not_recognized` 的页）：

```
\(O(M_0, \delta) = \{(x, y) \in \mathbb{R}^2 \mid (x - x_0)^2 + (y - y_0)^2 < \delta^2\}\)
\(O'(M_0, \delta) = \{(x, y) \in \mathbb{R}^2 \mid |x - x_0| < \delta, |y - y_0| < \delta\}\)
```

`\mathbb{R}^2`、`\varepsilon`、`\cdots`、`\mid` 全部正确，**裸 Unicode 数为 0**。

> 定位：**慢没关系，因为它只需要跑被规则筛出来的那 4,187 页**（占全库 11.84%），
> 不是全库 35,363 页。
> 4,187 页 × 70 s ÷ 24 并发 ≈ **3.4 小时**，免费。
>
> 但**要解决一个前置问题**：它没有 grounding 坐标 ⇒ 重抽时必须**只取文本、
> 保留原页面的图片位置**（图片从 `analysing/extraction/assets/` 取）。

---

## 四、文字模型质检实测（`work/_inject_probe.py` + `_inject_rescore.py`）

### 4.1 方法：受控注入

从 **10 本不同的书**各取 1 页纯文本页，人工注入 8 类错误，共 60 份样本 ×
**4 个不同家族的文字模型**（Qwen2.5-32B / Qwen3-32B / DeepSeek-V3 /
Seed-OSS-36B，两个 SiliconFlow key 轮转）= **240 次调用**。
**不给原文**（真实场景没有 ground truth），问"这页有没有 OCR 错误"。

注入类别：少字 3 字 / 多字 3 字 / 形近错字 ×2 / 连续逗号 `，，，` /
孤立尖括号 `< >` / 删中段 18%（大错）/ 中段 80 字替换乱码（大错）。

### 4.2 ⚠️ 我第一版判据是错的，这里推翻

第一版用 `has_error` 这个**布尔值**判分，得到"**漏报 0% / 误报 100%**"，
看起来像在说"文字模型毫无判别力"。**这个结论是错的，判据本身有问题。**

把 `issues` 内容打出来才发现，模型说的是：

| 注入内容 | 模型的回答 |
|---|---|
| 重复插入「微积分」 | 「在 '微积分微积分(上册)' 中，'微积分' 重复」 |
| 「分」改成「份」 | 「在 '数学份析的方法及例题选讲' 中的 '份' 应为 '分'」 |
| 插入「，，，」 | 「在 '裴，，，礼文' 中有多余逗号」 |

**全部准确命中并定位。** 而它对**未注入**原文报的是
「'Ⅱ' 应为罗马数字 'II'」这类**无害排版差异** —— 所以"误报 100%"的真实含义是
**门槛太宽**（把风格问题也算作"有错误"），不是"乱报"。

用布尔值判分，等于把真信号和噪声一起压平成 0/1。**这是我这轮最主要的错误。**

### 4.3 重新判分（只看 issues 有没有指对地方）

| 模型 | 小错命中率 | 大错命中率 | 原文误报（已收紧到 OCR 类指控） |
|---|---|---|---|
| **Qwen3-32B** | **90%** (29) | 67% (18) | 70% (10) |
| **Qwen2.5-32B** | **88%** (24) | 43% (14) | 57% (7) |
| **Seed-OSS-36B** | **87%** (30) | **90%** (20) | 100% (10) |
| **DeepSeek-V3** | **83%** (30) | 70% (20) | 90% (10) |

* 小错 = 少字/多字/错字/多余符号；大错 = 大面积缺字/乱码。
* **命中率 83~90% 是真实的、可用的信号** —— 主人最初"用文字模型检查文本"的方向成立。
* **但误报率仍高（57~100%）** ⇒ **不能单独用它筛页**，否则会把大量正常页拉进来。

### 4.4 正确的组合

```
确定性规则（高精度、零成本）筛出候选页  →  文字模型复核（高召回）
```

* 规则负责"别漏掉明显的大片问题"，且**价格为零、不会幻觉**；
* 模型负责"在候选页里认出具体的字级错误"——实测能指到字。
* **多模型投票的价值**：单模型误报 57~100%，投票可显著降误报
  （这是"多个文字模型并发"真正说得通的地方——**不是为了召回，是为了降误报**）。

---

## 五、确定性规则层：已建成，全库 174 本约 2 分钟、¥0

`data-src-page` 标在**每个段落块**上，所以必须区分**块**与**页**两个口径
（174 本 = **35,363 真实页** / **457,824 段落块**，比值 12.95）。**缺陷率一律以真实页为分母。**

| 层 | 脚本 | 覆盖 | 命中页 | 占比 |
|---|---|---|---|---|
| A 结构/公式 | `text_defect_census.py` | 公式未识别、定界符、`&lt;` 残留、中文混入公式、上下标、md 泄漏 | **4,187** | **11.84%** |
| C 书内自洽 | `text_selfconsistency.py` | 整页/部分公式丢失、页过短、页内定界符、上下标风格冲突 | **2,580** | **7.30%** |
| D 字符冗余 | `text_repeat_scan.py` | 双字词重复、单字×3、拉丁词重复、标点重复/混用 | **676** | 0.20% |
| E 书写一致 | `text_latexstyle.py` | 同书内少数派写法（`\epsilon`/`\varepsilon` 等） | 见 json | — |

另发现 **7,264 页完全没有文本块**（manifest 真实页 − 有文本页），含合法纯图页，需人工判断。

**关键**：`\dots` vs `\ldots` 这类**渲染完全相同**的不一致属于"源不一致但读者无感"，
按"对读者是否有影响"再分一层能砍掉大量无效工作。

---

## 六、结论汇总

| 问题 | 结论 |
|---|---|
| PaddleOCR-VL 并行提速？ | ❌ 慢 6 倍（70 s vs 12 s），与 DPI 无关；且丢版面坐标 |
| 2 key × 2 模型 = 4 通道叠加？ | ❌ 第二条通道只 +13 页/分，同期 DeepSeek 单臂能到 68 页/分 |
| 该往哪加？ | ✅ 继续加 **DeepSeek-OCR** 的并发（测 32/48/64 路），或换 MinerU |
| PaddleOCR-VL 有什么用？ | ✅ **精修引擎**：只跑规则筛出的 4,187 页，质量实测很好 |
| 文字模型从上下文查错？ | ✅ **命中率 83~90%**，能指到具体的字 |
| 单靠文字模型筛页？ | ❌ 误报 57~100%，必须规则先行 |
| 多模型并发？ | ✅ 价值在**降误报**（投票），不在提召回 |
| 需要原页图对照吗？ | ⬜ 优先级最低：文字模型的上下文推断已经能覆盖大部分，图文 diff 成本高、判断还更模糊 |

---

## 七、待办

1. 测 **DeepSeek-OCR 32/48/64 路的扩展性** —— 决定生产并发上限（当前 24 路）。
2. 把 A/C/D 三层合并成**一份去重的页级清单**，按严重度排序 → 交给文字模型复核。
3. 设计 **PaddleOCR-VL 精修流程**：文本替换但保留原页图片位置（用 assets）。
4. 用 E 层结论做一次**等价性判定**（哪些写法渲染相同 → 降级为无害）。
5. **口径修正要回灌**：早前记录里的"1.33% 缺陷率"是**块级**误算，正确页级是 **11.84%**。
