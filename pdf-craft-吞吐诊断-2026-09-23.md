# pdf-craft 要干多久？两个 API key 有没有用？

**日期**：2026-09-23 15:0x · **结论**：剩余 **37.5 小时（约 1.6 天）**；第二把 key **有用，且现在已经投产**。

---

## 一、直接回答

### Q1：pdf-craft 还要干多久？

| | 改前 | 改后（现在） |
|---|---|---|
| 配置 | `--jobs 12 --slice 8` · 1 把 key | `--jobs 24 --slice 48` · 2 把 key |
| 实测吞吐 | **12~17 页/分**（2.33~2.83 本/小时） | **62.5 页/分 = 89,994 页/天** |
| 剩余 140,702 页 | **6~10 天** | **≈ 37.5 小时 ≈ 1.6 天** |

**提速 4~5 倍**。而且改后的实测值（62.5 页/分）与事前理论预测
（24 路 × 60/22.6 秒 = 63.7 页/分）**误差仅 2%**，说明这个模型是可信的。

### Q2：我不是有两个硅基流动 API 吗？

**有用，但它的作用点你可能想错了。** 两把 key 不是"各自更快"，而是
**让并发数可以翻倍**——因为瓶颈根本不是"每个请求多快"，而是"同时在跑几个请求"。

而且在此之前，**第二把 key 根本没有接进生产**：`batch.py` 只读 `siliconflow.key`，
`siliconflow2.key` 一直躺在 `secrets` 目录里没人用。**现已接入**（见第四节）。

---

## 二、慢在哪：把时间拆到栈帧

对生产中的 worker 做 py-spy 采样：

```
46  93.9%  read (ssl.py:1138)              ← 等 HTTP 响应
 2   4.1%  wrapper (batch.py:429)          ← 文件操作
 1   2.0%  render_page (handler.py:156)    ← poppler 渲染
```

原始栈进一步卡在 `_read_status (http\client.py:292)` —— **请求已经发完，在等服务器开始回话**。

- 本地渲染只占 **2%**，**94% 是在等服务端推理**。
- 机器侧同时实测：24 worker 时 CPU 合计仅 **34%**（18 核）、磁盘读 0.1 MB/s、写 3.3 MB/s
  ⇒ **本机完全不是瓶颈**。

而 autopilot 日志里每本书都印着真实单价：**11.69 / 12.68 / 12.93 / 13.73 / 15.13 / 15.17 秒/页**。

**关键点**：pdf-craft **逐页串行**（`pdf_craft/pdf/ocr.py:107` 的 for 循环），
**没有任何并发参数**。所以——

> **单个进程的产能上限 = 60 ÷ 单页延迟 ≈ 4 页/分。想快，只能同时跑更多进程。**

---

## 三、加并发到底能加到多少（`work/_bench_real.py`，生产同款载荷）

| 配置 | 并发 | 墙钟 | 吞吐 | 单请求中位 | 成功 |
|---|---|---|---|---|---|
| key1 | 1 | 13.8s | 4.4 页/分 | 13.8s | 1/1 |
| key1 | 4 | 29.8s | 8.1 页/分 | 29.7s | 4/4 |
| key1 | 8 | 22.0s | 21.8 页/分 | 19.9s | 8/8 |
| key1 | 12 | 24.5s | 29.4 页/分 | 22.2s | 12/12 |
| 两把 key | 6+6 = 12 | 26.1s | 27.6 页/分 | 20.6s | 12/12 |
| **两把 key** | **12+12 = 24** | 30.1s | **47.9 页/分** | **22.6s** | **24/24 · 零 429** |

**读法**：单请求延迟在 1 → 24 路之间几乎不变（13.8 → 22.6 秒），
**吞吐严格正比于并发数**，全程**一次限流都没有**。

⚠ 注意一行反直觉的数据：12 路时「单 key 29.4 页/分」≈「两 key 27.6 页/分」。
说明 **12 路以内 key 的数量不起作用**（那一档的瓶颈是延迟，不是配额）；
**要到 24 路，第二把 key 才真正变成必需的**。

---

## 四、据此做的三处改动（均已生效并验收）

### ① 并发 8→24，切片 8→48（`work/watchdog.py`）

旧配置是 **「8 本书喂 12 个并发槽」**——从第一秒就有 4 个槽空着，而且书跑完一个少一个，
轮尾还会继续空转。新配置让队列里**始终有 24 本在跑 + 24 本待命**。

（`--slice` 必须 ≥ `--jobs` 的 2 倍：`batch.py` 用 `ThreadPoolExecutor(jobs)` + `as_completed`，
队列一空并发就掉。）

### ② 第二把 key 接入生产（`pipeline/batch.py`）

- 新增 `api_keys()`：glob `siliconflow*.key`，去重保序；
- 按**书路径的稳定校验和**取模分配（不用 `hash()`——`PYTHONHASHSEED` 随机化会让同一本书
  每轮换 key，排查不可复现）；
- 通过环境变量 `SF_API_KEY` 下发给 worker ⇒ **零新增管道，密钥也不出现在进程列表里**。

验收（读进程真实环境变量，命令行里看不到）：

```
存活 worker = 24 个 · key 分布 {key1: 15, key2: 9}
```

### ③ 单本超时不再跟着预算缩小（`work/autopilot.py`）

原式 `--timeout-min = max(30, budget // 60)`。因为 autopilot 在 `run_batch` 里是
`p.wait()` **阻塞**的，deadline 到了也**不会**中断这一批 ⇒ 这个超时**唯一的实际效果**，
就是让"开在 3 小时预算尾段的那一轮"拿到 30 分钟，把一本 364 页（需要约 91 分钟）的书
**拦腰砍死**。

现改为 `max(60, 最大页数 × 16 ÷ 60 + 30)`，与预算无关。

---

## 五、顺带处理掉的两个问题

**MinerU 线已停用。** 其免费档按天配额已烧穿：`mineru_daily.json` = `budget 1800 / used 15765`（超 8.7 倍）。
症状是提交后**所有**文件永远停在 `pending`、3600 秒后认输回流 ⇒ **全批次零产出**，
还占着 `mineru_reserved.txt`（那里的书 pdf-craft 和 MinerU **都不碰**，是黑洞）。
已放回 **18 本 / 5,071 页**，队列 **257 本 / 135,631 页 → 275 本 / 140,702 页**。
配额按天重置，需要复活时把 `watchdog.py` 里那行取消注释即可。

**生产线的看护从 6 条线收敛到 5 条**：autopilot(→24 worker) · refill_maas · refill_ms ·
sf_qa · vlm_retry_fix。

---

## 六、两个有待观察的点

1. **24 路是持续跑，bench 只是 30 秒突发** —— 若硅基流动对持续高频调用降级，
   表现为单请求延迟上升或 429，届时把 `--jobs` 回调到 16 并观察。
2. **`ode.pdf`** 稳定报 `TypeError: '<=' not supported between instances of 'int' and 'NullObject'`
   （PDF 页面树里有 NullObject），需要单独修，否则它会一直占着一个并发槽。
