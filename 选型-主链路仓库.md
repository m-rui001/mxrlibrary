# 选型：已有路线里最好的仓库

> 结论日期：2026-09-20 ｜ 约束：STEM 类 PDF（含扫描件）→ EPUB / HTML，公式须走 MathML

## 结论

**`oomol-lab/pdf-craft` 是最好的仓库。** 它不是「某一段最好」，而是**唯一一个把整条链路都做完了、而且每一段都留了替换口**的项目。

```
https://github.com/oomol-lab/pdf-craft      MIT · v2.2.0（2026-09-09）· 最后提交 2026-09-18
```

---

## 一、它为什么胜出

| 判据 | pdf-craft 的表现 |
|---|---|
| 能否直接出 EPUB | ✅ 唯一一个。带目录、书元数据、脚注（含脚注内图片）、表格 |
| 能否出 MathML | ✅ `latex_render=LaTeXRender.MATHML` |
| 是否面向扫描件 | ✅ 项目定位原文就是 "focus on processing PDF files of scanned books" |
| 许可 | ✅ MIT（对比：Marker GPL-3.0 + 权重 Open RAIL-M；MinerU AGPL-3.0） |
| 活跃度 | ✅ v2.2.0 于 2026-09-09 发布，最后一次提交 2026-09-18（两天前），243 commits |
| 是否需要 GPU | ✅ **不需要**（见下，远程 OCR） |
| 是否离线可用 | ✅ 本地 OCR 模式全程无网络请求 |

---

## 二、2026-09 的三处重要变化（⚠️ 修正上一版调研结论）

### 1. API 全部更换 —— 旧写法已失效

上一版调研引用的 `PDFPageExtractor` / `generate_epub_file` 是旧 API。**当前写法：**

```python
from pdf_craft import PDFCraft, PDFOptions, DeepSeekOCRVendorConfig

craft = PDFCraft(pdf=PDFOptions(
    ocr=DeepSeekOCRVendorConfig(
        base_url="https://your-endpoint/v1",   # 兼容 DeepSeek OCR 的服务
        api_key="...",
        model="deepseek-ocr",
    ),
))
craft.convert_pdf_to_epub("input.pdf", "output.epub")
```

拆分式（推荐，见第四节）：

```python
extraction = craft.extract_pdf("book.pdf", "work/book.pcex")   # 抽取一次
craft.render_epub(extraction, "book.epub",
                  table_render=TableRender.HTML,
                  latex_render=LaTeXRender.MATHML,
                  inline_latex=False)                          # ⚠️ 默认 True，见第三节
```

### 2. 「公式抽取需要 CUDA」已不再成立

现在 OCR 支持 **3 个模型 × 本地/远程 = 6 种配置**：

| 模型 | 本地 | 远程 |
|---|---|---|
| DeepSeek OCR | ✅ | ✅ |
| DeepSeek OCR 2 | ✅ | ✅ |
| Unlimited OCR（百度） | ✅ | ✅ |

- **远程 OCR 不需要本地 CUDA**（README 原文：remote OCR "does not require local CUDA"），只需 Python 3.11–3.13 + Poppler + 一个兼容服务地址。
- 本地 OCR 才需要另装 `pdf-craft[local]`、CUDA 版 PyTorch、足够显存与模型文件。

**这条修正很重要**：原先把「必须上本地 GPU」列为门槛，实际上现在可以先用远程跑通再决定要不要自建。

### 3. EPUB 下游被抽成了独立包 —— 这是最大的架构红利

`LaTeXRender` / `TableRender` 现在不在 pdf-craft 里，而来自**独立 PyPI 包 `epub-generator`**：

```
https://pypi.org/project/epub-generator/    0.1.0 · MIT · 作者 Tao Zeyu（与 pdf-craft 同一作者）
```

- 接口是「Markdown/文本 + LaTeX 数学 → EPUB」，枚举确认存在 `LaTeXRender.MATHML = "mathml"`、`TableRender.HTML = "html"`。
- **测试依赖 epubcheck 校验生成的 EPUB** —— 对 EPUB 结构合法性有硬要求，这点在同类项目里少见。

**含义**：你可以用 Marker / Docling / rustypaper / 任何工具做前端，只把 `epub-generator` 拿来做 EPUB 打包层。这正是上一轮建议的「分层解耦」，而它已经现成给你了。

### 4. 附带的 `.pcex` 抽取归档

`extract_pdf()` 产出一个 `.pcex` 文件，可落盘、可跨机器复用，之后分别渲染 Markdown / EPUB / 翻译版，**不用重跑 OCR**。这就是「统一中间表示作为契约」的工程化落地。

---

## 三、仍然存在的坑（选它也要防）

1. **`inline_latex=True` 是 EPUB 默认值** —— 行内公式保留为 LaTeX 源码，指望阅读器渲染，而几乎没有 EPUB 阅读器带数学 JS 渲染器。**STEM 书里必须显式关掉或改走 MathML/SVG。**
2. **`epub-generator` 还是 0.1.0** —— 成熟度明显低于 pdf-craft 本体。它的 MathML 产出质量尚无公开评测，属于需要自己验证的未知项。
3. **不背书阅读器兼容性** —— 旧文档明确写过 MathML 在 EPUB 2.0 不受支持、"not all EPub readers can render the formula correctly"，并建议 SVG 才通用。
4. **HTML5 容错 ≠ XHTML 严格** —— 畸形公式在浏览器能糊过去，在 EPUB 的 XHTML 里是硬解析错误。（`epubcheck` 校验能拦住一部分。）
5. EPUB 的 `lan` 参数目前只有 `zh` / `en`。
6. 走 PDF 回写（翻译版 PDF）另需 Ghostscript + Qt/PySide6，与 EPUB 路线无关，别被 README 带偏。

---

## 四、为什么不是其他

| 仓库 | 为什么不选为主链路 |
|---|---|
| **Docling**（IBM，MIT，⭐20K+） | **不能写 EPUB**。官方输出格式清单里只有 JSON / YAML / Markdown / HTML / Split-page HTML / Text / DocTags / WebVTT / Chunks / LaTeX —— 它能*读* EPUB，但不能*写*。只能当前半段。它的 MathML 走 `latex2mathml`。 |
| **datalab-to/marker**（公式最强） | 官方 README 明确：HTML 输出「与 Markdown 类似，公式以 `$$` 定界符包裹」—— **不产出 MathML**，仅当识别器用。且 GPL-3.0 + 权重 Open RAIL-M。 |
| **MinerU** | AGPL-3.0，传染性强；输出 Markdown / JSON，无 EPUB。 |
| **lgmoneda/pdf-to-epub** | 不是主链路候选，但**它的 benchmark 套件 + 可选 LLM 质量判官值得直接抄**来做评测体系。 |
| **pdf2htmlEX** | 版面保真、非语义数学；原仓库已停止维护。 |
| **marker 公式精度参考** | olmocr-bench 76.0% 总 / 83.5% born-digital —— 这是目前可得的最高公开分数，可作为 pdf-craft 的对照基线。 |

> 注意同名陷阱：`fabiocolacio/Marker` 是 GTK Markdown 编辑器，与 PDF 转换的 `datalab-to/marker` 无关。

---

## 五、推荐接法

1. **主链路用 pdf-craft 的远程 OCR 模式**先跑通，避免一开始就陷进本地 CUDA 环境。
2. **用 `.pcex` 作为契约**，把「抽取」和「渲染」彻底分开，抽取只做一次。
3. **接缝 ①（OCR）**：先用 DeepSeek OCR 基线，再按公式准确率换 Unlimited OCR / 本地模型。
4. **接缝 ②（MathML）**：先试内置 `latex_render=MATHML`；若产出质量不足，在这个缝上换成 **Temml**（MIT，面向 MathML Core，Node 无头可跑），因为接口两边都是「LaTeX 字符串 ↔ MathML」，替换成本低。
5. **务必把 `inline_latex` 设为 `False`**，否则行内公式退化成裸 LaTeX 源码。
6. **加一层 epubcheck 门禁**（`epub-generator` 本身已经在用），拦住畸形 MathML 导致的 XHTML 硬错误。
7. **评测抄 lgmoneda/pdf-to-epub** 的固定测试集 + 期望项 + LLM 判官闭环。

---

## 六、什么情况下换掉它

- 源以**数字版 PDF** 为主、且不需要 EPUB → Docling 的元素级溯源（页码 + bbox）更适合做后续加工。
- 只要**最高公式识别精度**、能接受 GPL-3.0 → Marker。
- 只有扫描件且**完全不要公式**（纯散文）→ AnClark/PDF2Epub_2 更轻。
